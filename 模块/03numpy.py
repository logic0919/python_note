import numpy as np
'''
np.array()和对应的np.shape
    一维数组：np.array([1, 2, 3])                                  (3,)
    行向量：np.array([[1, 2, 3]])                           	     (1, 3)
    列向量：np.array([[1], [2], [3]])                              (3, 1)
    矩阵：np.array([[1,2],[3,4]])                                 (2, 2)
    三维数组：np.array([[[1,2],[3,4]], [[5,6],[7,8]]])             (2, 2, 2)
np.dot()
    一维数组：得到标量（线代中的内积计算）：
    行向量列向量：得到仅有一个数字的二维矩阵：
    两个二维矩阵：
    三维数组：abc和dce，a和d就是表示批次，本质依然是批量二维矩阵相乘
        “笛卡尔积”配对
        三维升四维：bc和ce组成be作为后面两个维度，前两个维度是ad
                  也就是说abc和dce组成adbe
        对于 (1,2,3) × (1,3,4)，结果形状一定是 (1,1,2,4)
        
        维度提升：当不满足以上三个条件，比如a和d分别是3和2，那么就会变成32be，变成四维，此时两个批次维度都被保留，做“笛卡尔积”配对
    四维数组：
@和np.matmul：两者等效，它们之间是“语法糖”和“函数”的关系，就像 + 和 np.add 一样
    二维：对于二维矩阵，它们的行为和dot完全一样。真正的区别在三维及以上的数组。
    三维：（二维无批次，从三维开始才有批次）
        计算方法：
            把最后两维当作矩阵，前面的所有维度都作为批次（Batch），进行“对应批次”的矩阵乘法。
            广播：两种情况，分别是a=d或者a=1或者d=1，三种情况分别得到abe或者dbe或者abe
                (b,m,n) @ (b,n,p) → (b,m,p) —— 批次维度相同，一对一对应。
                (b,m,n) @ (1,n,p) → (b,m,p) —— 右矩阵批次为1，广播到左矩阵的批次。
                (1,m,n) @ (b,n,p) → (b,m,p) —— 左矩阵批次为1，广播到右矩阵的批次。
                (b,m,n) @ (c,n,p) —— 若 b ≠ c 且均不为1，则报错。
    四维：
        (a,b,m,n) @ (a,b,n,p) → (a,b,m,p)
        (a,1,m,n) @ (1,b,n,p) → (a,b,m,p)
二维矩阵相乘中，@和dot和matmul结果一样，它们的区别在于高维（三维及以上）
高维 @ 只做“一对一”或“一对多（广播）”的批量矩阵乘法，绝不会增加维度；而 np.dot 会做“多对多”的配对，导致维度提升。


问题：
    1.
        三维数组相乘的“笛卡尔积”配对具体过程展示
        import numpy as np
        A = np.random.rand(3, 4, 5)  # 3个矩阵，每个 4x5
        B = np.random.rand(2, 5, 6)  # 2个矩阵，每个 5x6
        result = np.dot(A, B)
        print(result.shape)  # 输出 (3, 2, 4, 6)
        给出以上代码的直观表示视频（三维升成四维）
'''

